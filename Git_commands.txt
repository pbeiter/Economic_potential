
git config --global user.name "your-name"

git config --global user.email "your-email"

git config --list // to check if Git is already configured 
---
ADD NREL ACCOUNT:

git remote -v
git remote add NREL git@github.nrel.gov:pbeiter/OSW-Econ-potential.git
git remote add NREL https://github.nrel.gov/pbeiter/OSW-Econ-potential.git

git push NREL master (--force)
--

git status

git init

git add

git commit -m “Add Readme.txt”

git remote add origin https://github.com/username/myproject.git //establish remote repository
git remote -v //check whether remote origin has been added

git push -u origin master

git log


git clone git@github.com:pbeiter/Economic_potential.git

